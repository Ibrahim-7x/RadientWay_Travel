# dependencies
node_modules/

# build output
dist/

# vite cache
.vite/

# env / secrets (keep .env.example tracked)
.env
.env.local
.env.*.local
server/.env

# databases
*.db
*.db-journal

# server uploads (keep the folder, not its contents)
server/uploads/*
!server/uploads/.gitkeep

# logs & OS files
*.log
.DS_Store
Thumbs.db
